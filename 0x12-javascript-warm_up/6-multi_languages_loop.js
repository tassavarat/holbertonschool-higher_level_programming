#!/usr/bin/node

/* Prints 3 lines by using an array of strings and a loop */
const array = ['C is fun', 'Python is cool', 'Javascript is amazing'];

for (let i = 0; array[i]; ++i) {
  console.log(array[i]);
}
